name: signature_uploader
description: "A new Flutter project."
publish_to: 'none' 
# This should match the version and shortversion in app-archive.json
# Update every new app version
version: 1.0.2+3

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  file_picker: ^10.0.0
  path: ^1.9.1
  image: ^4.5.4
  
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
